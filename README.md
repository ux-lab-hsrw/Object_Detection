1. Run these commands on you terminal

pip install numpy
pip install opencv-python
pip install tensorflow
pip install keras


2. Provide path of image and video in detectImage, detectVideo files

3. Run sample.py for testing